# 交接文档 · 构建「销售行动屏」BI Webapp

> 面向：接手实际开发这个 webapp 的编码 agent。
> 目标：把已设计好的「销售行动屏」做成能连数仓、可运行的自研 BI Demo。
> 本文只做导航与关键约束，**不复制**已有文档内容——细节见下方引用文件（均在项目文件夹 `HM_AI Native Solution Breakdown/`）。

---

## 1. 要构建什么

一个**移动优先的销售经营 BI Webapp（"销售行动屏"）**：前端 React + ECharts，通过一个**只读权限 API** 从数据仓库取数并按身份做行级过滤。这是华美食品集团 AI Native 转型 Phase A 的自研 BI POC。

三个必须验证的点：真数据驱动行动屏、行级权限随身份切换生效、观感（对标并超越商业 BI）。

## 2. 当前状态

- 数据仓库已建立，已拉简单表单测试通过，现进入 BI demo 测试阶段。
- 全部为设计/规格文档 + 静态原型；**尚无运行代码**。下一步就是把原型接真数据变成可跑 webapp。
- Demo 阶段简化：**先用模拟身份切换**（客户经理/大区经理/高层，如现有原型），**暂不接飞书 SSO**；跑通后再接。

## 3. 关键设计约束（务必遵守）

- **项目宪法（最高原则）**：AI 只呈现事实 + 建议动作类型，**不下真因结论、不评价人**。行动清单每条＝"客观事实 + 建议动作"，真因判断留给销售。
- **行级权限**：一律下推到数据库引擎/API 层，**不在前端手写行过滤**；API 用**只读**数据库角色；找不到身份映射即拒绝、不给默认权限。
- **行动导向第一屏（不是通用 KPI）**：双引擎——季节品冲刺条（达成/缺口/距节点/需日均）+ 日消品健康条（活跃客户/铺市率）+ 今日行动清单（断货/掉量/呆滞/回款，各带考核标签+建议动作）。标准描述型指标放二屏。
- **华美 VI**：主色橙红 `#FF6C00`、深紫 `#3C0082`、辅色粉 `#FFD2F0`、黄 `#FFFF00`；中文字体汉仪旗黑、英文 Bw Gradual（授权字体，web 用系统字体 + Poppins 近似，生产再挂正式字体）。

## 4. 主要参考文件（按用途，勿重复其内容）

| 用途 | 文件 |
|---|---|
| **实施主文档**（SQL 清单、只读 API 规格、数据契约、字段映射、验收） | `华美_销售行动屏_BI_Demo实施指南.docx` (TECH-010) |
| 交互与布局参考（可直接改造的原型） | `华美_销售行动屏_手机第一屏原型.html` |
| VI 品牌版视觉参考 | `华美_销售经营看板_MVP_品牌版.html` |
| 指标口径与待确认项 | `华美_销售自助看板指标字典.xlsx` |
| 行级权限数据模型/可见性视图 | `华美_行级权限引擎设计规格.docx` (#13) |
| 行动/异常规则（规则管道，非 agent） | `华美_异常发现能力设计规格.docx` (#20) |
| 数据平台选型（PolarDB PG 方向） | `华美_数据平台技术栈决策更新_v1.1.docx` |
| 后续：飞书 SSO 接入 | `华美_飞书接入与SSO对接方案.docx` (#10) |
| 后续：ChatBI（高层 deep dive） | `华美_ChatBI_Agent设计规格.docx` (#19) |
| POC 整体框架与对比 | `华美_自研BI_POC方案书.docx` |

> `实施指南 (TECH-010)` 是编码的主入口：内含每个卡片/行动项的可跑 SQL（PostgreSQL 风格）、`v_emp_visible_cust` 行级过滤视图、`GET /api/action-board` 端点与返回 JSON 契约、前端改造点、6 条验收用例。

## 5. 开工前必须向用户拿到的两项（阻塞 SQL 落地）

1. **数仓库类型**：PolarDB PostgreSQL 还是仍在 SQL Server（决定 SQL 方言：视图、日期函数、SESSION_CONTEXT vs JWT）。
2. **销售相关表名与关键字段结构**（出库、客户主数据含归属 owner_emp/owner_org、库存、季节目标）。
   拿到这两项即可把 TECH-010 里的通用 SQL 改成贴合真实表的可跑版本。

## 6. 其余待确认（见 TECH-010 第十节 / 指标字典「待业务确认」）

季节品目标数据来源与颗粒度、客户归属是否干净、各阈值（断货天数/掉量周数与降幅/呆滞周数）、库存与铺市/新品清单是否具备。这些决定哪些模块能上、规则怎么触发。

## 7. 建议的构建顺序

1. 用模拟身份 + mock 数据先把前端行动屏跑起来（复用原型 HTML 的布局与 VI）。
2. 搭只读 API 骨架 + `v_emp_visible_cust` 行级过滤；接真数仓（拿到库类型+表结构后落 SQL）。
3. 逐模块接真数据：季节冲刺 → 日消健康 → 行动清单四类规则。
4. 跑 TECH-010 的 6 条验收用例（重点：切身份可见范围变、越权返回空、API 只读）。
5. Demo 通过后再接飞书 SSO、加 ChatBI、与商业 BI 基准对比提交治理委员会定板。

## 8. 建议调用的技能（Suggested Skills）

- **`anthropic-skills:web-artifacts-builder`** — 构建这个多组件 React + Tailwind 前端 webapp 的主力技能（状态管理、组件化、图表）。
- **`anthropic-skills:docx`** — 若需回写/更新实施指南等规格文档。
- **`anthropic-skills:xlsx`** — 若需更新指标字典/字段映射表。
- （可选）**`data:sql-queries` / `data:write-query`** — 将 TECH-010 的通用 SQL 适配到实际库方言、优化查询。

## 9. 注意

- 全部产出保存到项目文件夹 `HM_AI Native Solution Breakdown/`；临时/中间产物用 outputs 暂存。
- 不要在前端或 API 引入任何写数据能力（Demo 纯只读）。
- 保持"事实 + 建议动作"的呈现，勿让前端/agent 生成"真因结论"或对人的评价（红线）。
